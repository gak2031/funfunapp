version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:7
    working_directory: ~/temp

    steps:
      - checkout
      - run npm install
      - run npm test

 

      

      
    
